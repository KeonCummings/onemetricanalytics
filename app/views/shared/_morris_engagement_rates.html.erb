<script>
  Morris.Line({
    element: 'line',
    data: [
    <% metrics_history_eur(params[:id], 'page_consumptions', 'page_posts_impressions', 60).each do |k,v|%>
      { date: '<%= raw k%>' , value: <%= v %>},
    <% end %>
    ],
    xkey: 'date',
    ykeys: ['value'],
    labels: ['Engaged User Rate'],
    parseTime: false,
    lineColors: ['#FF3D0D']
  });
</script>